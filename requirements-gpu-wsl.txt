# Use on WSL2 Ubuntu after: pip install "tensorflow[and-cuda]>=2.16.0"
# (Same as requirements.txt but without plain tensorflow — keeps NVIDIA CUDA wheels.)
#
# Do NOT list torch here: default `pip install torch` pulls CUDA 13 wheels that
# conflict with TensorFlow's CUDA 12 stack. Install CPU-only torch in the script
# after TF (FinBERT/transformers do not need GPU for this project).
numpy>=1.26.0
pandas>=2.1.0
yfinance>=0.2.31
matplotlib>=3.8.0
seaborn>=0.13.0
scikit-learn>=1.3.2
arch>=6.2.0
statsmodels>=0.14.1
scipy>=1.12.0
tqdm>=4.66.1
pytrends
transformers>=4.36.0
